---
applyTo: "**"
---

When managing packages for this project, always use `pnpm` as the package manager. This ensures consistency across development environments and leverages `pnpm`'s efficient handling of node modules.
